#!/bin/bash
BF_ROOT=$(git rev-parse --show-toplevel)
TA_DIR=$BF_ROOT/threads-analysis
URL=$1

cd $TA_DIR

pairs() {
  for source_a in data/sources/*; do
    for source_b in data/sources/*; do
      echo $source_a $source_b
    done
  done
}

pairs | parallel -j 20 bin/gen-sim-ent-int {}
