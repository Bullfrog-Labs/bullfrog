version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - node/test:
          name: run-tests
          pkg-manager: yarn
          setup:
            - CI=true
          run-command: yarn workspaces run test
          cache-version: v3
